{
  "name": "aunbox-doc-start-dev",
  "version": "1.0.0",
  "description": "",
  "main": "gulpfile.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "gulp templates",
    "prepare": "cd dist && bower install && cd ..",
    "start": "npm run prepare && npm run build && docute dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gulp-autoprefixer": "^3.1.1",
    "gulp-rename": "^1.2.2",
    "gulp-ruby-sass": "^2.1.1",
    "gulp-twig": "^0.7.0"
  },
  "dependencies": {
    "gulp": "^3.9.1"
  }
}
