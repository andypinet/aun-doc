{
  "name": "aunbox-doc-start-dev",
  "version": "1.0.0",
  "description": "",
  "main": "gulpfile.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "gulp templates",
    "start": "npm run build && docute dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gulp-rename": "^1.2.2",
    "gulp-twig": "^0.7.0"
  },
  "dependencies": {
    "gulp": "^3.9.1"
  }
}
